{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",

  "name": "${mod_name}",
  "description": "Lock content behind stages driven by datapacks",
  "authors": ["${mod_author}"],
  "contact": {
    "sources": "https://github.com/${repository}",
    "issues": "https://github.com/${repository}"
  },

  "license": "CC0-1.0",

  "environment": "*",
  "entrypoints": {
    "main": [
      "com.possible_triangle.stages.fabric.FabricEntrypoint"
    ],
    "jei_mod_plugin": [
      "com.possible_triangle.stages.features.StagesJEI"
    ]
  },
  "mixins": ["${mod_id}.mixins.json"],

  "depends": {
    "fabricloader": ">=0.14",
    "fabric": "*",
    "minecraft": "1.20.x",
    "java": ">=17"
  }
}
